<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <queries>
        <package android:name="com.instagram.android" />
        <package android:name="com.discord" />
    </queries>

    <application
        android:name=".ConstantiaApplication"
        android:allowBackup="true"
        android:label="Constantia"
        android:supportsRtl="true"
        android:theme="@style/Theme.Constantia">
        <receiver android:name=".core.notifications.NotificationAlarmReceiver" android:exported="false" />
        <receiver android:name=".core.notifications.NotificationDismissReceiver" android:exported="false" />
        <receiver android:name=".core.focusgate.FocusGateRecheckReceiver" android:exported="false" />

        <service
            android:name=".core.focusgate.FocusGateVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="false" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="local_per_app_focus_gate_that_discards_network_packets_for_user_selected_distraction_apps" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
